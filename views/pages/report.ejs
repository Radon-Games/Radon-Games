<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../components/head") %>
  </head>
  <body>
    <%- include("../components/nav") %>

    <section class="w-full max-w-2xl m-10 px-6 py-4 mx-auto">
      <h2 class="text-3xl font-semibold text-center text-gray-200">Bug Report</h2>
      <span class="text-red-500"><% if(locals.error) { %><%= error %><% } %></span>
      <span class="text-green-700"><% if(locals.message) { %><%= message %><% } %></span>
      <form class="mt-6" method="POST" action="/report">
        <div class="items-center -mx-2 md:flex">
          <div class="w-full mx-2">
            <label class="block mb-2 text-sm font-medium text-gray-200">Name</label>
            <input name="name" class="block w-full px-4 py-2 border rounded-md bg-gray-800 text-gray-300 border-gray-600 focus:ring-blue-300 focus:border-blue-300 focus:outline-none focus:ring focus:ring-opacity-40" type="text"/>
          </div>

          <div class="w-full mx-2 mt-4 md:mt-0">
            <label class="block mb-2 text-sm font-medium text-gray-200">E-mail</label>
            <input name="email" class="block w-full px-4 py-2 border rounded-md bg-gray-800 text-gray-300 border-gray-600 focus:ring-blue-300 focus:border-blue-300 focus:outline-none focus:ring focus:ring-opacity-40" type="email"/>
          </div>
        </div>

        <div class="w-full mt-4">
          <label class="block mb-2 text-sm font-medium text-gray-200">Message<span class="text-red-500">*</span></label>
          <textarea name="message" class="block w-full h-40 px-4 py-2 text-gray-300 bg-gray-800 border rounded-md border-gray-600 focus:ring-blue-300 focus:border-blue-300 focus:outline-none focus:ring focus:ring-opacity-40">
Name:
URL/Game:
Version: <%= version %>
Priority: (High/Medium/Low)
Date: <%= (new Date().getMonth()+1) + "/" + new Date().getDate() + "/" + new Date().getFullYear() %>

Description:


Steps to Reproduce:


Expected Result:
</textarea>
        </div>

        <div class="flex justify-center mt-6">
          <input type="submit" class="px-4 py-2 text-gray-200 transition-colors duration-200 transform bg-gray-700 rounded-md hover:bg-gray-600 focus:outline-none focus:bg-gray-600" value="Report"/>
        </div>
      </form>
    </section>
    
    <%- include("../components/footer") %>
  </body>
</html>